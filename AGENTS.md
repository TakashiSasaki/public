# 🤖 AGENTS.md

このファイル(`AGENTS.md`)は、このプロジェクトに関わる**すべての人間の開発者およびAIコーディングエージェント**が参照し、遵守すべき指示・ルールをまとめた構造化ドキュメントです。
プロジェクトでの作業を開始する前に、必ずこのドキュメントを通読し、記載された方針に従ってください。

---

## 1. プロジェクト概要 (Project Overview)

- **プロジェクト名**: `mouse-pointer`
- **目的**: Windows用のマウスポインタをカスタマイズ・生成するためのプログラムやファイルの作成。
- **主な成果物**:
  - `*.cur`（静的カーソルファイル）の生成と、それを支援するツール。
  - `*.ani`（動的カーソルファイル）関連ツールの開発。
  - カスタマイズ作業を容易にするCLIおよびGUI（Tkinter）アプリケーション。
- **GUI構成**: `src/mouse_pointer/gui.py` をエントリポイントとし、各タブのロジックは `src/mouse_pointer/ui/tabs/` (`editor_tab.py`, `export_tab.py`, `fonts_tab.py`) に分割されています。

## 2. 開発環境と技術スタック (Environment & Stack)

- **言語**: Python (>=3.12推奨)
- **パッケージ・依存関係管理**: `uv`
  - パッケージの追加や同期には `uv add`, `uv sync` を使用してください。
  - Pythonスクリプトの実行は、標準的なPythonインタープリタを介して行うために `uv run python <script>` または `uv run <command>` を使用してください。

## 3. バージョン管理とコミットのルール (Version Control & Commits)

当プロジェクトでは、コミットごとに自動化されたバージョン採番（セマンティックバージョニングのパッチレベル更新）を義務付けています。

- **バージョンソース**: プロジェクトのバージョンは `pyproject.toml` 内の `[project]` セクションにある `version` フィールドが単一のソースです。
- **Gitフックの有効化 (Recommended)**:
  - 当プロジェクトでは、Gitフックを `githooks/` ディレクトリで管理し、リポジトリ上でトラッキング可能にしています。
  - 以下のコマンドを実行して、このディレクトリをGitフックのパスとして設定してください。
    ```bash
    git config core.hooksPath githooks
    ```
  - これにより、`githooks/pre-commit` が自動的に実行され、コミット時のバージョンバンプアップと同期が保証されます。

## 4. エージェントへの指示 (Directives for Agents)

AIエージェントとしてこのプロジェクトでコードの修正や機能追加を行う際は、以下のルールを厳守してください。

1. **環境の尊重**: カスタムスクリプトの実行や依存関係の解決には常に `uv` エコシステムを使用すること。
2. **ルールの継承**: 新しい機能を追加した場合でも、既存のフォーマットやCLI/GUIの起動・統合方法（`src/mouse_pointer/` 以下の構成）の構造を壊さないこと。
3. **フックの維持**: `pyproject.toml` を直接編集して手動でバージョンを書き換えることは避け、バージョン管理は基本としてGitフック（設定されている場合）に委ねること。

## 5. SVG レンダリングと透過処理 (SVG Rendering & Transparency)

ピクトグラムの表示や合成に `svglib` および `reportlab` を使用していますが、これらのライブラリは PNG 出力時のアルファチャンネル（透過）を直接正しく扱うことができません。

そのため、以下の **Black/White Matte 技法** を採用しています。

- **仕組み**:
    1. 背景を「白」にしてレンダリングを行う。
    2. 背景を「黒」にしてレンダリングを行う。
    3. 両者のピクセル値の差分から、本来のアルファ値（透過度）を計算して合成する。
- **実装箇所**:
    - `src/mouse_pointer/gui.py` (`render_svg_thumbnail`)
    - `src/mouse_pointer/generators/basic.py` (`render_svg_to_pil`)

**注意点**:
- 新規に SVG レンダリング処理を追加・修正する場合は、必ずこの技法が適用されているか確認してください。単に `bg=None` や `bg=0x00000000` を指定するだけでは背景が透過されず、黒または白の矩形が残ってしまいます。

## 6. フォント処理とフィルタリング (Font Processing & Filtering)

フォント一覧タブでは、システムフォントの特性に応じた高度な処理を行っています。

- **等幅 (Monospace) 判定**:
  - `ImageFont.truetype` で読み込んだ後、小文字の 'i' と大文字の 'W' の描画幅 (`getlength`) を比較するヒューリスティックを採用しています。
- **ビットマップ・固定サイズフォント (.fon) の対応**:
  - Windows の `.fon` ファイルなどは特定のピクセルサイズしかサポートしていないため、任意のサイズ指定で `OSError: invalid pixel size` が発生することがあります。
  - **解決策**: 読み込みに失敗した場合、サポートされているサイズ（8〜20辺り）を総当たりで検索して読み込み、その後 Pillow の `resize(..., Image.Resampling.NEAREST)` で要求されたサイズへスケーリングします。これにより、ドット感を維持したまま拡大・縮小表示が可能です。
- **フィルタリング**:
  - `pixel`, `dot`, `bitmap` などのキーワードを含むファイル名をビットマップフォントとして優先的にフィルタリングします。

## 7. バージョン情報の取得 (Version Retrieval)

- アプリケーションのバージョンは `pyproject.toml` に定義されています。
- GUI等の実行時には `importlib.metadata.version("mouse-pointer")` を優先して参照するように実装してください。

