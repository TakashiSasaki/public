Janome のユーザー辞書（CSV）ファイルは **MeCab 互換形式** で記述する必要があります。以下のような **CSV フォーマット** で単語を定義できます。

### **ユーザー辞書のCSVフォーマット**
```csv
表層形,左文脈ID,右文脈ID,コスト,品詞,品詞細分類1,品詞細分類2,品詞細分類3,活用型,活用形,原形,読み,発音
```
- **表層形**：実際に出現する単語の形
- **左文脈ID / 右文脈ID**：通常 `0`（MeCab の文法情報と関連）
- **コスト**：単語のコスト（値が低いほど優先的に選ばれる）
- **品詞**：名詞、動詞、形容詞など（例: `名詞`）
- **品詞細分類**（1～3）：品詞の細かい分類（例: `固有名詞`, `一般` など）
- **活用型 / 活用形**：動詞や形容詞の活用情報（通常 `*`）
- **原形**：辞書に登録する単語の基本形
- **読み**：カタカナでの発音表記
- **発音**：発音情報（通常 `*`）

---

### **ユーザー辞書の具体例**
例えば、「ChatGPT」や「OpenAI」などの単語を適切に分割させるための辞書を作るとします。

```csv
ChatGPT,0,0,5000,名詞,固有名詞,一般,*,*,*,ChatGPT,チャットジーピーティー,チャットジーピーティー
OpenAI,0,0,5000,名詞,固有名詞,一般,*,*,*,OpenAI,オープンエーアイ,オープンエーアイ
```

### **ユーザー辞書の登録方法**
1. **CSVファイルを作成**  
   `user_dict.csv` というファイルを作成し、上記のように単語を登録。
   
2. **Python で Janome を初期化するときにユーザー辞書を読み込む**
```python
from janome.tokenizer import Tokenizer

# ユーザー辞書を指定
tokenizer = Tokenizer(udic="user_dict.csv", udic_enc="utf8")

text = "ChatGPTはOpenAIが開発した人工知能です。"
tokens = [token.surface for token in tokenizer.tokenize(text)]
print(tokens)  # ['ChatGPT', 'は', 'OpenAI', 'が', '開発', 'した', '人工知能', 'です', '。']
```

---

### **ポイント**
- コスト値（5000など）は適切に調整する必要があります。  
  - **値が低い** → 優先的に選ばれる（例: 2000-5000）  
  - **値が高い** → 他の解析候補と競合しやすい（例: 10000 以上）  
- **エンコーディング** を `utf8` にすることで、多言語に対応しやすくなる。

この方法を使うことで、**標準の Janome では分割されてしまう単語を、指定の形で認識させることが可能** です。