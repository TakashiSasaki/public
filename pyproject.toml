[project]
name = "get-a-grip"
version = "0.1.15"
description = "A directory searching application"
authors = [
    {name = "takas"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[project.scripts]
get-a-grip = "get_a_grip.cli:main"


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
