[project]
name = "file-catalog"
version = "0.1.17"
description = "Desktop inventory and search tool for Windows."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "platformdirs>=4.0.0",
]

[project.scripts]
file-catalog = "file_catalog.main:main"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

