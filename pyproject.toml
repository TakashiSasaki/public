[project]
name = "git-detect-related-branch"
version = "0.2.19"
description = "Git Branch Detector GUI Tool"
requires-python = ">=3.8"
authors = [
    { name = "Takashi Sasaki" }
]
dependencies = []

[project.urls]
Homepage = "https://x.com/TakashiSasaki"

[project.scripts]
gui = "gag.repo_viewer.detect_gui:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/gag"]
